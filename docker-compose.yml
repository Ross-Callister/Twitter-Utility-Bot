version: "3.8"

services:
  bot:
    build: .
    volumes:
      - ./data:/app/data
      - ./downloads:/app/downloads
    environment:
      - DISCORD_TOKEN=${DISCORD_TOKEN}
    restart: unless-stopped
